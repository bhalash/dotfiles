# Linux: generally, I just want a smaller font size to fit the laptop display

font:
  size: 9.0

window:
  startup_mode: Fullscreen

key_bindings:
  - { key: F, mods: Shift|Control, action: ToggleFullscreen }

import:
  - ~/.dotfiles/alacritty/base.yml
